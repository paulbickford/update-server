#!/bin/sh

# Disable Erlang distribution
# export RELEASE_DISTRIBUTION=none

# Allow remote iex session to connect to node.
# iex --sname "debug@maussaki" --cookie my_erlang_cookie --remsh "update_server@maussaki"
export RELEASE_DISTRIBUTION=name
export RELEASE_NODE=<%= @release.name %>@maussaki